# Backend
PORT=5000
JWT_SECRET=replace_with_a_long_random_secret

# If password has special characters (like #), URL-encode them.
# Example: RonitSoni12# should be RonitSoni12%23
MONGODB_URI=mongodb+srv://username:password@clustertest.h0y0aph.mongodb.net/lms?retryWrites=true&w=majority&appName=ClusterTest

# Optional frontend override:
# VITE_API_BASE_URL=http://localhost:5000/api
